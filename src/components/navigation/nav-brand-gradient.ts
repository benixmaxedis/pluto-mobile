import { colors } from '@/lib/theme';

/** Actions → Capture → Routines (matches Now tab bar flash / label). */
export const NAV_BRAND_GRADIENT = [
  colors.actions.primary,
  colors.capture.primary,
  colors.routines.primary,
] as const;
