/** 8pt grid: prefer multiples of `unit` for vertical rhythm. */
export const spaceUnit = 8;

export const spacing = {
  /** 4px — hairlines only */
  hairline: 4,
  /** 8px — 1 unit */
  xs: 8,
  /** 16px — 2 units */
  sm: 16,
  /** 24px — 3 units */
  md: 24,
  /** 32px — 4 units */
  lg: 32,
  /** 40px — 5 units */
  xl: 40,
  /** 48px — 6 units */
  '2xl': 48,
  /** 64px — 8 units */
  '3xl': 64,
} as const;
